""" Techniques for task recommendation

Task recommender protocol: domain, chunk, student -> task (or None)
"""
import random

def select_random(domain, chunk, student):
    raise NotImplementedError
