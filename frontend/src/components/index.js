/**
 * Index of pure React components that can be packages independently.
 */
import FieldBackground from '../components/FieldBackground';
import SpaceWorld from '../components/SpaceWorld';

export default {
  FieldBackground,
  SpaceWorld
};
